name: CD Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Pull latest code and deploy
        run: |
          cd /home/albert/log430-labo1-E26
          git pull origin main
          docker compose down
          docker compose build
          docker compose up -d